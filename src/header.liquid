        <header>
            <h1>Schema Host</span>{% if object %} - <span itemprop="name">{{ object.name }}</span>{% endif %}</h1>
            <h2>Validatable HTML microdata schemas</h2>
        </header>